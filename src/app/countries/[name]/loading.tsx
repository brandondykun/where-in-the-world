const Loading = () => {
  return (
    <main className="flex flex-1 justify-center bg-very-light-gray dark:bg-very-dark-blue-bg items-center">
      <div className="text-3xl animate-pulse">Loading...</div>
    </main>
  );
};

export default Loading;
